<header>
  <h1> List of Products </h1>
  <p class="products">We have {{content.length}} products</p>
  <p class="products">The time now is {{time}}</p>
</header>
  <div class="listing">
    {{#each content}}
      <ul>
        <li>Item: {{title}}{{#link-to 'product' this class='btn btn-success' tagName='button'}}Details{{/link-to}}</li>
        Item on Sale?
        {{#if onSale}}
          yes
        {{else}}
          no
        {{/if}}
        Cost Analysis?
        {{#if expensive}}
          expensive item!
        {{else}}
          {{#link-to 'products.deals'}} Here are some Deals {{/link-to}}
        {{/if}}
      </ul>
    {{/each}}
  </div>
</div>

<div class="outlet"> {{outlet}} </div>

<div class="addnew">
  <h2>Add New Product</h2>
  <div>
    Title: {{textarea valueBinding='title'}}
    <p>{{title}}</p>

    Description: {{textarea valueBinding='description'}}
    <p>{{description}}</p>

    Price: {{textarea valueBinding='price'}}
    <p>{{price}}</p>

    On Sale: {{textarea valueBinding='onsale'}}
    <p>{{onsale}}</p>

    <button {{action 'createProduct'}}>Submit New Product</button>
  </div>
</div>
